apiVersion: v1
kind: Service
metadata:
  name: sre-demo
  labels:
    app: sre-demo
spec:
  type: NodePort
  selector:
    app: sre-demo
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      # NodePort para acesso local via localhost (30080)
      nodePort: 30080
