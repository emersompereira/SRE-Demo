kibanaConfig:
  kibana.yml: |
    server.host: "0.0.0.0"
    elasticsearch.hosts: ["https://elasticsearch-master:9200"]
    # Trocamos Token por Basic Auth
    elasticsearch.username: "elastic"
    elasticsearch.password: "T0eStDvFMS7Km3Rt"
    elasticsearch.ssl.verificationMode: none

service:
  type: NodePort
  nodePort: 32561